(()=>{const SB=window.supabase.createClient('https://qvxoxrzvxyyribqjnpkb.supabase.co','sb_publishable_1zOA0YpTtJkNYsmm4zXxsA_wSJpdHKe');const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function roundNo(v){const n=String(v??'').match(/\d+/);return n?Number(n[0]):0}async function generate(tid){const{data:existing}=await SB.from('matches').select('id').eq('tournament_id',tid).limit(1);if(existing?.length){alert('この大会にはすでに試合があります。');return}const{data:teams,error}=await SB.from('teams').select('id,name,seed').eq('tournament_id',tid).in('status',['approved','pending']).order('seed',{ascending:true,nullsFirst:false}).order('created_at');if(error)throw error;if(!teams?.length||teams.length<2){alert('2チーム以上登録してから生成してください。');return}const size=Math.pow(2,Math.ceil(Math.log2(teams.length)));const slots=Array.from({length:size},(_,i)=>teams[i]||null);const total=size-1;const rows=[];for(let r=1;r<=Math.log2(size);r++){const count=size/Math.pow(2,r);for(let i=0;i<count;i++){const a=r===1?slots[i*2]?.id:null,b=r===1?slots[i*2+1]?.id:null;rows.push({tournament_id:tid,round_name:String(r).padStart(2,'0'),round_number:r,team_a_id:a,team_b_id:b,score_a:0,score_b:0,status:(a&&b)?'scheduled':'scheduled'})}}const{data:inserted,error:ie}=await SB.from('matches').insert(rows).select('id,round_number,created_at');if(ie)throw ie;const byRound={};(inserted||[]).forEach(m=>(byRound[m.round_number]??=[]).push(m));for(const r of Object.keys(byRound).map(Number).sort((a,b)=>a-b)){const cur=(byRound[r]||[]).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)),next=(byRound[r+1]||[]).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at));for(let i=0;i<cur.length;i++)if(next[Math.floor(i/2)])await SB.from('matches').update({next_match_id:next[Math.floor(i/2)].id}).eq('id',cur[i].id)}}async function openBracket(){const app=$('app'),title=$('page-title');title.textContent='トーナメント';app.innerHTML='<div class="panel">読み込み中…</div>';try{const{data:{user},error:ae}=await SB.auth.getUser();if(ae||!user)throw new Error('ログインしてください');let tq=SB.from('tournaments').select('id,name,format').order('created_at',{ascending:false});const{data:profile}=await SB.from('profiles').select('role').eq('id',user.id).maybeSingle();if(profile?.role!=='super_admin')tq=tq.eq('owner_id',user.id);const{data:ts,error:te}=await tq;if(te)throw te;if(!ts?.length){app.innerHTML='<div class="panel empty"><h2>大会がありません</h2></div>';return}const t=ts[0];const{data:ms,error:me}=await SB.from('matches').select('id,round_name,round_number,team_a_id,team_b_id,score_a,score_b,status,winner_team_id,created_at').eq('tournament_id',t.id).order('created_at');if(me)throw me;const ids=[...new Set((ms||[]).flatMap(m=>[m.team_a_id,m.team_b_id]).filter(Boolean))];let teams=[];if(ids.length){const r=await SB.from('teams').select('id,name,tag').in('id',ids);if(r.error)throw r.error;teams=r.data||[]}const tm=new Map(teams.map(x=>[x.id,x]));const rounds={};(ms||[]).forEach(m=>{const r=m.round_number||roundNo(m.round_name)||1;(rounds[r]??=[]).push(m)});const nums=Object.keys(rounds).map(Number).sort((a,b)=>a-b);const finalRound=nums.length?Math.max(...nums):0;const final=finalRound?(rounds[finalRound]||[])[0]:null;const champion=final?.status==='completed'?tm.get(final.winner_team_id):null;const label=r=>r===finalRound?'FINAL':`ラウンド${String(r).padStart(2,'0')}`;const matchHtml=(m,i)=>{const a=tm.get(m.team_a_id),b=tm.get(m.team_b_id),done=m.status==='completed',winner=m.winner_team_id;return `<div class="amida-match"><div class="amida-team ${winner===m.team_a_id?'winner':''}"><span>${esc(a?.name||'未定')}</span><b>${done?m.score_a:'-'}</b></div><div class="amida-bridge"></div><div class="amida-team ${winner===m.team_b_id?'winner':''}"><span>${esc(b?.name||'未定')}</span><b>${done?m.score_b:'-'}</b></div></div>`};app.innerHTML=`<div class="section-head"><div><span class="eyebrow">SINGLE ELIMINATION</span><h2>${esc(t.name)}</h2><p class="muted">勝者が次のラウンドへ自動進出します。</p></div><div><button class="secondary" id="br-refresh">↻ 更新</button>${!ms?.length?'<button class="primary" id="br-generate" style="margin-left:8px">＋ トーナメント生成</button>':''}</div></div>${champion?`<div class="panel champion"><div class="eyebrow">TOURNAMENT CHAMPION</div><h2>🏆 ${esc(champion.name)}</h2><p class="muted">優勝チームが決定しました。</p></div>`:''}${!ms?.length?'<div class="panel empty"><h3>トーナメントが未生成です</h3><p class="muted">登録チームから4・8・16・32チーム規模に合わせて自動生成します。</p></div>':`<div class="amida-scroll"><div class="amida-board">${nums.map(r=>`<section class="amida-round"><h3>${label(r)}</h3><div class="amida-matches">${rounds[r].map(matchHtml).join('<div class="amida-gap"></div>')}</div></section>`).join('<div class="amida-connectors"><span></span></div>')}</div></div>`}`;const refresh=$('br-refresh');if(refresh)refresh.onclick=openBracket;const gen=$('br-generate');if(gen)gen.onclick=async()=>{gen.disabled=true;try{await generate(t.id);await openBracket()}catch(e){alert('生成エラー: '+e.message);gen.disabled=false}}}catch(e){console.error(e);app.innerHTML=`<div class="panel error"><h3>トーナメント表を読み込めませんでした</h3><p>${esc(e.message)}</p></div>`}}document.addEventListener('click',e=>{const b=e.target.closest('.nav-item[data-view="bracket"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();document.querySelectorAll('.nav-item[data-view]').forEach(x=>x.classList.remove('active'));b.classList.add('active');openBracket()},true);const s=document.createElement('style');s.textContent='.amida-scroll{overflow-x:auto;padding:18px 0 30px}.amida-board{display:flex;align-items:stretch;min-width:max-content;padding:10px 24px}.amida-round{width:250px;position:relative}.amida-round>h3{text-align:center;margin:0 0 18px;font-size:14px}.amida-matches{display:flex;flex-direction:column;justify-content:space-around;min-height:420px}.amida-match{position:relative;margin:18px 0}.amida-team{height:42px;display:flex;align-items:center;justify-content:space-between;gap:10px;padding:0 12px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.035);border-radius:9px;font-size:13px}.amida-team.winner{font-weight:700;border-color:rgba(123,239,255,.55)}.amida-team.winner span:before{content:"✓ ";}.amida-team b{min-width:18px;text-align:right}.amida-bridge{height:28px;width:54px;margin-left:auto;border-right:2px solid rgba(123,239,255,.45);border-bottom:2px solid rgba(123,239,255,.45);border-radius:0 0 8px 0}.amida-connectors{width:54px;position:relative;min-height:420px}.amida-connectors:before{content:"";position:absolute;left:0;right:0;top:50%;border-top:2px solid rgba(123,239,255,.38)}.amida-connectors:after{content:"";position:absolute;left:50%;top:25%;bottom:25%;border-left:2px solid rgba(123,239,255,.38)}.amida-gap{height:8px}.champion{margin-bottom:18px;text-align:center}.champion h2{margin:8px 0}';document.head.appendChild(s)})();